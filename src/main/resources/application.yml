spring:
  profiles:
    active: local

  application:
    name: shoppingMall

  thymeleaf:
    cache: false

  jpa:
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    p6spy: info
